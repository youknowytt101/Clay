# ADR-004 治理回放 · r1

| | |
|---|---|
| **状态** | **未通过** —— G1 与 G3 通过；G2 漏掉 OR 组合语义。首次失败作为证据保留，ADR-004 继续处于“倾向已定” |
| **日期** | 2026-08-09 |
| **执行** | 三个隔离上下文的只读执行者；主流程只负责预置夹具、统一评分和证据归档 |
| **上游** | [ADR-004](adr-004-evidence-governed-evolution.md) §12；[goals.md](goals.md) 决策 42 |
| **原始指标** | [`evidence/adr-004-replay-r1.json`](evidence/adr-004-replay-r1.json) |

---

## 0. 结论

首轮回放不能把 ADR-004 转为“已验证”：

| 回放 | 结果 | 关键遗漏召回 | 无效挑战 | 越权修改 |
|---|---|---:|---:|---:|
| G1 文档漂移 | 通过 | 7 / 7 | 0 | 0 |
| G2 隐含语义 | **失败** | **5 / 6** | — | 0 |
| G3 机制自挑战 | 通过 | champion 5 / 5；Lean-8 2 / 5 | 两者均 0 / 3 | 0 |

G2 的漏项不是实现问题，而是治理 champion 的真实盲区：十二面覆盖能找到对象关联、平距、销毁可见性、
稳定顺序和数值精度，却没有强迫执行者检查条件组合的 AND / OR / NOT、量词、短路与优先级。

---

## 1. 方法与隔离

三个执行者均使用空白上下文，只收到各自夹具、四态 CoverageCell 规则和输出契约；不读取仓库、历史结论或预期答案，
也没有文件写权限。主流程在执行前固定植入项，返回后按原子遗漏计分，不用模型数量或措辞裁决。

- **G1 夹具**：四份互相冲突的摘要，覆盖目标用户、模板范围、ADR 状态和路线图下一步。
- **G2 夹具**：只给“两座塔同一 tick 各打离自己最近的敌人”和确定性硬要求，不提示六个植入点。
- **G3 夹具**：三个关键遗漏任务与三个无问题对照；champion 为 TaskContract + D1–D12 + 负空间，
  challenger `Lean-8` 删除 D7 / D10 / D11 / D12，并跳过重构类任务的负空间扫描。

---

## 2. G1 · 文档漂移回放

执行者发现全部七个原子冲突：目标用户、模板数量、ADR-001 两处状态、ADR-002 状态、M0-a 状态，
以及同一 roadmap 内互斥的两个“下一步”。它按 `goals > ADR > outline / roadmap > README` 裁决可裁决项，
没有按多数文档选答案；对 roadmap 内部同权威冲突保持 `open`。

输出的 `DecisionChallenge` 明确区分了“陈旧投影”和“尚未登记的上游推翻”，建议先保护高权威意图，
只生成不落盘的字段对照与候选修复清单。仍可继续的工作包括链接、版本、状态引用和断链统计。越权修改为 0。

---

## 3. G2 · 隐含语义回放

执行者主动发现五个植入点：

1. 每座塔必须保留自己的来源对象与目标关联，不能把候选集合全局化；
2. 等距敌人需要稳定 tie-break，不能依赖容器顺序；
3. 同 tick 必须定义共同快照、销毁可见性与共同目标处理；
4. 事件和日志需要规范稳定顺序；
5. 距离度量、坐标空间、浮点精度、NaN / Infinity 与采样时点需要确定。

它还给出无平距基础案例、等距阻塞案例、共同目标死亡案例和可检出距离变化的负例。

**漏判**：没有主动提出多条件组合的 OR 语义，包括 OR 的左右分支看到哪个实体集、短路是否影响副作用、
AND / OR 优先级和“任一 / 全部 / 不存在”的量词边界。因此植入遗漏召回只有 5 / 6，G2 判失败。

---

## 4. G3 · Champion / Lean-8

| 指标 | Champion | Lean-8 |
|---|---:|---:|
| 关键遗漏召回 | 5 / 5（100%） | 2 / 5（40%） |
| 对照任务无效挑战 | 0 / 3 | 0 / 3 |
| 越权修改 | 0 | 0 |
| 六任务检查单元 | 84 | 54 |

Lean-8 少 30 个检查单元，但漏掉序列化 id 的旧数据 / 依赖迁移、1000 单位缓存的规模退化，
并弱化了超时自动提交对用户控制的检查。它没有满足同一 hard criteria，因此拒绝采用并回退到 champion。
小改可以预填带理由的 `not-applicable`，但不能删除决策面或负空间。

---

## 5. 首个真实纵切

M1-e 跨 CPU 闸门完整走过一次有效挑战、一次无效挑战拦截和证据复现：

- **有效挑战**：初版跨机器 JSON 只锁依赖与场景，没有证明两边运行同一 spike / Transform 实现；
  采用前加入换行归一的两份源码哈希，再运行云端对拍。
- **无效挑战拦截**：同 CPU 浏览器或 QEMU 只能作预检，不能替代 hard gate 要求的真实不同 CPU 架构；
  没有降低判据，改用 GitHub Actions ARM64 runner。
- **证据复现**：[run 31320198471](https://github.com/youknowytt101/Clay/actions/runs/31320198471) 通过，
  两份原始 JSON 已入库并由 [`tests/gate-a-evidence.test.mjs`](../../tests/gate-a-evidence.test.mjs) 对拍。

---

## 6. DecisionChallenge · 逻辑运算符探针

- **Challenged ref**：ADR-004 §5 的十二面 champion 与 §14 最小执行模板。
- **Trigger / hidden decision**：G2 漏掉 OR；D1–D12 没有明确要求规则系统检查逻辑组合、量词、短路与优先级。
- **Why it matters now**：事件表求值语义属于 I11 冻结边界；静默采用一种 OR 语义会让同一作品产生不同结果。
- **Options**：A）在规则 / 表达式 / 状态机任务的 D1–D2 下增加条件式逻辑探针；B）新增普适 D13；C）不改 champion，只靠领域作者记住。
- **Current evidence**：r1 的 G2 为 5 / 6；G1、G3 和三个无问题对照没有越权或无效挑战。
- **Recommendation**：A。它只对存在条件组合的任务启用，预计比普适 D13 负担小，也比依赖记忆可审计。
- **Smallest reversible probe**：用相同 G2 夹具在新隔离上下文影子复跑，再用三个无问题对照测无效挑战。
- **Work that may continue**：保存 r1、准备 challenger 与对照；不修改 L0–L1，不将 ADR-004 转为“已验证”。
- **Authority needed**：项目所有者批准是否把该探针纳入治理 champion；AI 不能自行采用。

---

## 7. Evidence package

- **Completed**：G1 / G2 / G3 首轮隔离回放；M1-e 真实纵切的挑战与复现核对。
- **Not completed**：G2 未过；ADR-004 不转状态；新探针尚未获批、尚未影子复跑。
- **Failures**：OR 组合语义漏判 1 项，关键遗漏召回 5 / 6。
- **U-NNN**：新增 `U-047`；本轮未关闭未知项。
- **Champion / challenger**：Lean-8 拒绝并回退；逻辑运算符探针仅为待批准 challenger。
- **Reproduction**：`node --test tests/governance-evidence.test.mjs`；原始计分见 JSON。
- **Next deadline**：M0-d / ADR-004 G2 第二轮回放前。
