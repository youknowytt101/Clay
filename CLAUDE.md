# Clay · 给 AI 的工作说明

**这是一个设计阶段的文档仓库，尚无代码。** 全部内容是 Markdown + 一张 SVG + 一份生成的预览页。

---

## 权威顺序

改动或回答问题时，按这个顺序认账：

| 优先级 | 文件 | 是什么 |
|---|---|---|
| **1** | `docs/design/goals.md` | **最高裁决依据**。决策表 + 不变量 + 范围 + 架构 + 闸门。**任何冲突以它为准** |
| 2 | `docs/design/adr-00N-*.md` | 单点技术选型。goals.md 只引用结论 |
| 3 | `docs/design/architecture-outline.md` | goals.md 的**投影**：决策后的能力清单，只写「是什么」 |
| 3 | `docs/design/roadmap.md` | goals.md §6 的**施工版**：包 / 依赖 / 体量 / 决策截止点 |
| 4 | `docs/design/ai-native-engine.md` | AI 基础设施规格，goals.md 的下级文档 |

`docs/conventions/`、`docs/architecture/` 是**从 E:\GameHub 继承的存量参考**（已冻结）。
文档里形如 `../../js/core/History.js` 的链接指向那个仓库，在本仓库内断链是**有意的**。

---

## 五条编辑规则（goals.md §0.5，对全部文档有效）

1. **不写工期、预算、人力。** 只写引擎与编辑器该是什么样子。需要排期感就用相对体量（S/M/L/XL）
2. **决策表只增不改。** 推翻旧决策要新增一行 `| ↳ | | **vN 修订/推翻**：… | |`，**不要静默改写原行**
3. **判据必须可判定。** 不可测的说法一律换掉
4. **单点技术选型另开 ADR**（`docs/design/adr-NNN-*.md`），goals.md 只引用结论
5. **每次结构性改动更新版本号**，在 goals.md 开头写明这一版确立了什么，并在 §0.6 版本沿革表**追加一行**

---

## 改完文档必须做的两件事

```bash
python tools/check-docs.py
```
```bash
python tools/build-preview.py
```

- **`preview.html` 是生成物，永远不要手改**——下次构建会覆盖
- 新增一份文档要同时加进 `tools/build-preview.py` 的 `DOCS` 列表，否则它不会出现在预览里
- `check-docs.py` 报错就修，不要忽略

---

## 最容易漏的一处

**推翻一条决策时，要扫全仓库，不只是 `docs/design/`。**

v17 推翻决策 12（双模式）和 19（用户画像）时就漏了 `docs/conventions/ui.md`——
那里有一整节叫「双模式靠令牌表达」，引用的是已被取消的机制。

改动涉及决策编号时，先跑：

```bash
grep -rn "决策 N" --include="*.md" .
```

---

## 当前状态

- **版本**：goals.md v18
- **目标游戏**（范围纪律的判据源，goals.md §3.2）：
  俯视指令簇 = 红警 / 星际 / 魔兽 / Dota 2；第三人称动作簇 = 塞尔达 / 艾尔登 / 刺客信条 / TPS
- **护城河**：I1 + I3（AI 与人共用同一条编辑通道，产出同一种材料）
- **下一步**：ADR-002 的三个手工样例推演 → 冻结求值语义（唯一不可逆项，纸面工作）

**不要往设计文档里加新范围**，直到 README「下一步」的三件事做完——
现在的决策密度已经超前于实现进度。
